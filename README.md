# miau

download PHP > Environment Variables > “Edit the system environment variables” > add PATH > php -v
download Composer Setup > composer -v

LARAVEL
composer global require laravel/installer
laravel --version

laravel new projectname (none, 0, no, mysql, no, no)
cd projectname
php artisan serve
